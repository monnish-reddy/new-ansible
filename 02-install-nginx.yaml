- name: install nginx on frontend
  hosts: frontend
  become: yes
  #connection: local
  tasks:
  - name: install nginx on frontend
    ansible.builtin.package:
      name: nginx
      state: installed
  - name: start nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enable: yes
    
